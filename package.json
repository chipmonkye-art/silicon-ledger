{
  "name": "silicon-accounting-v2",
  "private": true,
  "scripts": {
    "build": "cd silicon-accounting-v2 && npm install && npm run build && npx prisma generate && npx prisma db push --accept-data-loss",
    "start": "cd silicon-accounting-v2 && node dist/index.js"
  }
}
